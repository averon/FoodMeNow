<nav class="navbar">
  <div class="navbar-header">
    <a class="navbar-brand" href='#'>FoodMeNow</a> 
  </div>
  <ul class="nav navbar-nav pull-right">

    <li class="dropdown">
      <div class="btn-group">
        <button type="button" class="btn" data-toggle="modal" data-target="#checkout">
          Checkout
        </button>
        <button type="button" class="cart-button btn btn-primary" data-toggle="dropdown">
          <span class="glyphicon glyphicon-shopping-cart"></span>
          Cart
        </button>
        <ul class="order-cart dropdown-menu dropdown-menu-right">
        </ul>
      </div>
    </li>

    <li>
      <% if (FoodMeNow.currentUser) { %>
        <a href='#'>Sign out</a>
      <% } else { %>
        <a href='#'>Sign in</a>
      <% } %>
    </li>
  </ul>
</nav>

